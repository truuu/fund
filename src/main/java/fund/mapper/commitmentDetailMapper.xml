<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="mapper.commitmentDetailMapper">
 <select id="selectByCommitmentID" resultType="fund.commitmentDetail"> 
 	SELECT c2.*, c3.codeName
 	FROM commitment c1 JOIN  commitmentDetail c2 ON c1.id=c2.commitmentID JOIN code c3 ON c2.bankID=c3.id
 	WHERE c3.id={id} 
 	</select>
 
 </mapper> 